---

---

<header class="l-header">
  <div class="l-header__inner">
    <h1><a href="/">BLOG</a></h1>
    <!-- <nav class="l-header__nav">
      <ul class="l-header__list">
        <li class="l-header__item"><a href="/">HOME</a></li>
      </ul>
    </nav> -->
  </div>
</header>

<style lang="scss">
  .l-header {
    position: relative;
    background-color: #333;
    @include media-pc {
      height: rem(60);
    }
    @include media-sp {
      height: vw(40);
    }
    &__inner {
      display: flex;
      justify-content: space-between;
      align-items: center;
      height: 100%;
      margin-inline: auto;
      @include media-pc {
        width: rem(1000);
      }
      @include media-sp {
        width: vw(335);
      }
    }
    h1 {
      a {
          color: #fff;
          font-weight: bold;
          letter-spacing: 0.07em;
        }
      @include media-pc {
        transition: scale 0.3s $easeInOutBack;
        a {
          font-size: rem(20);
        }
        &:hover {
          scale: 1.1;
        }
      }
      @include media-sp {
        a {
          font-size: vw(20);
        }
      }
    }
    // &__nav {
    // }
    // &__list {
    //   display: flex;
    //   flex-flow: row wrap;
    //   column-gap: 1em;
    //   justify-content: flex-end;
    //   align-items: center;
    // }
    // &__item {
    //   padding: 0.5em;
    //   a {
    //     color: #fff;
    //   }
    // }
  }
</style>
